# Large local datasets supplied separately
tables/
project-2-bnpl-tables-part*.zip

# Local Python environment and caches
.venv/
__pycache__/
*.py[cod]
.ipynb_checkpoints/

# Duplicate packaged deliverable (the source folder is committed)
member1_eda_bundle.zip

# Local database and operating-system files
*.duckdb
*.duckdb.wal
.DS_Store
Thumbs.db

# Large transaction fact table (14M+ rows) reproduced locally from the Canvas-supplied
# raw data; too large for GitHub, matches the pattern member2 already used for tables/.
member2_curation/data/curated/curated_transactions/
member2_curation/data/curated/quarantined_transactions.parquet
