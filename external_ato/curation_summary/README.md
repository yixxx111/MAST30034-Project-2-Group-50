# ATO curation summary

打开 `ato_summary.ipynb` 查看已执行的代码与结果；`ato_summary.html` 是不需要 Python 的图文版。
在小组仓库根目录运行 `python external_ato/build_summary.py` 可从当前结果重新生成并执行。
先安装 external_ato/requirements.txt；使用当前 Python 环境作为临时 notebook kernel，不修改全局 kernel。
